<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.2.1">Jekyll</generator><link href="github.com/emma-ea/feed.xml" rel="self" type="application/atom+xml" /><link href="github.com/emma-ea/" rel="alternate" type="text/html" /><updated>2023-04-23T11:42:44+00:00</updated><id>github.com/emma-ea/feed.xml</id><title type="html">trust the computer</title><subtitle>Just an experimental blog. Everything computer science.</subtitle><entry><title type="html">Explicit Animations in Flutter 2</title><link href="github.com/emma-ea/android,/flutter/2023/04/22/animating-widgets-flutter-part-2.html" rel="alternate" type="text/html" title="Explicit Animations in Flutter 2" /><published>2023-04-22T20:23:00+00:00</published><updated>2023-04-22T20:23:00+00:00</updated><id>github.com/emma-ea/android,/flutter/2023/04/22/animating-widgets-flutter-part-2</id><content type="html" xml:base="github.com/emma-ea/android,/flutter/2023/04/22/animating-widgets-flutter-part-2.html"><![CDATA[<p>In the previous post, I explained how to get started with explicit animation in Flutter. In this post, I’ll walk you through how to build a simple animation using all the classes I mentioned. We’re going to make a hand-waving animation using the wave hand emoji.</p>

<h3 id="setting-up-your-project">Setting up your project</h3>

<p>Since the app is intended to only run on Android, I’ll be using the flutter create command to only build the project for Android. Flutter builds a project for some default platforms(Android, IOS, Windows, Linux, Macos, Web); if you want to build for specific platforms, you can specify them using the platforms option.</p>

<p><code class="language-plaintext highlighter-rouge">flutter create --platforms android wave_hand_app</code></p>

<p>Clean up the default project template in your <code class="language-plaintext highlighter-rouge">main.dart</code> file, leaving only the <code class="language-plaintext highlighter-rouge">main</code> method and the <code class="language-plaintext highlighter-rouge">stateless widget class</code>. You should have something like this now afte converting your default class to a <code class="language-plaintext highlighter-rouge">stateful widget class</code>.</p>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">void main() {

    runApp(
        const MaterialApp(
            title: 'Wave',
            home: MyApp(),
        ),
    );

}

class MyApp extends StatefulWidget {

    const MyApp({super.key});

    @override
    State&lt;StatefulWidget&gt; createState() {
        return MyAppState();
    }

}

class MyAppState extends State&lt;MyApp&gt; {

    @override
    Widget build(BuildContext context) {
        return Text('hello world');
    }

}</code></pre></figure>

<h3 id="using-the-animation-classes">Using the animation classes</h3>

<p>As said in the previous post, to use the <code class="language-plaintext highlighter-rouge">animation controller</code> class to build explicit animation, we need to first use the <code class="language-plaintext highlighter-rouge">Ticker</code> mixin. This class helps send a notification to the animation controller that a new frame has been rendered to the screen. We will use the <code class="language-plaintext highlighter-rouge">SingleTickerProviderStateMixin</code> because we only have one animation controller we intend on using to manage our animation.</p>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">class MyAppState extends State&lt;MyApp&gt; with SingleTickerProviderStateMixin { 
    ....
}    </code></pre></figure>

<p>Once this is done, we can go ahead and create and initialize our animation objects. Create instance variables within the state class.</p>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">class MyAppState extends State&lt;MyApp&gt; with SingleTickerProviderStateMixin { 

    late AnimationController _waveController;
    late Animation _waveAnim;
    late Tween _waveTween;

    int waveDuration = 150;

    @override
    void initState() {
        super.initState();
        // vsync is a TickerProvider interface and requires a class that
        // uses the TickerProvider mixin
        _waveController = AnimationController(
            vsync: this, 
            duration: Duration(milliseconds: waveDuration),
        );
        // the begin and end values were justified by how much further 
        // i wanted the hand wave animation to go on the z-axis.
        _waveTween = Tween&lt;double&gt;(begin: -5.0, end: 30.0);
        _waveAnim = _waveTween.animate(_waveController);
    }


    @override
    void dispose() {
        // releases the resources used
        _waveController.dispose();
        super.dispose();
    }

    ....

}</code></pre></figure>

<h3 id="putting-together-the-needed-widgets">Putting together the needed widgets</h3>

<p>In this project, I made use of the <code class="language-plaintext highlighter-rouge">animated builder</code> widget, the <code class="language-plaintext highlighter-rouge">transform</code> widget, and a <code class="language-plaintext highlighter-rouge">text</code> widget to hold a wave hand emoji.</p>

<p><code class="language-plaintext highlighter-rouge">AnimatedBuilder</code> widget is a general-purpose widget for building animations. This widget provides a way to handle complex animations while making sure it doesn’t sacrifice performance, optimizing the animation as it renders.</p>

<p><code class="language-plaintext highlighter-rouge">Transform</code> widget applies transformation to its child before painting it on screen. In our use case, we will be applying a rotation on the z-axis to a <code class="language-plaintext highlighter-rouge">Text</code> widget and animating it back and forth; this will in turn create a hand wave animation.</p>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">Widget _buildWaveHand() {
    return Row(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
            Text(
                'Hello there',
                style: GoogleFonts.lexend(fontSize: 24),
            ),
            AnimatedBuilder(
                animation: _waveAnim,
                builder: (context, child) {
                    return Transform(
                        key: const Key('transformer'),
                        transform: Matrix4.identity()
                            ..rotateZ(_waveAnim.value.toRad),
                        alignment: Alignment.bottomRight,
                        child: child,
                    );
                },
                child: GestureDetector(
                    onTap: _startWaveAnim,
                    child: const Text(
                        key: Key('wave-emoji'),
                        style: TextStyle(fontSize: 24),
                    ),
                ),
            ),
        ]
    );
}

void _startWaveAnim() {
    final ticker = _waveController.repeat(reverse: true);
    ticker.timeout(
        Duration(milliseconds: waveDuration * 6),
        onTimeout: () {
            _waveController.stop();
            _waveController.animateTo(0);
        }
    );
}

extension on double {
    // you'll need to import dart:math to access pi.
    // rotateZ angle is required to be in radians
    double get toRad =&gt; this * (pi / 180);
}</code></pre></figure>

<p>In order to apply the transform, we use <code class="language-plaintext highlighter-rouge">rotateZ</code> from the <code class="language-plaintext highlighter-rouge">Matrix4</code> class. An <code class="language-plaintext highlighter-rouge">identity</code> is applied to the matrix to prepare for any transformation.</p>

<p>I have a text widget wrapped in a <code class="language-plaintext highlighter-rouge">gesture detector</code> that listens for tap events and triggers the animation to start when the event is detected.</p>

<p>The <code class="language-plaintext highlighter-rouge">_startWaveAnim</code> method uses the <code class="language-plaintext highlighter-rouge">repeat</code> method of the animation controller class, which repeats the animation indefinitely. In order to stop the animation after a certain duration, we use a <code class="language-plaintext highlighter-rouge">Ticker</code> returned by the repeat method to manually timeout the animation.</p>

<p>You can insert this <code class="language-plaintext highlighter-rouge">_buildWaveHand</code> in your widget tree and you should be good to go. Here we go; this is what I have. If you follow this post, the emoji on your device may be different. This is because each manufacturer has its own implementation of emoji ISO codes.</p>

<p><img src="/assets/wave_hand_anim.gif" alt="wave hand giff" /></p>

<h3 id="ending-here">Ending here</h3>

<p>So far, we have been able to build a simple wave hand animation, and I hope you enjoyed the journey. If I missed something or made any blunders, kindly reach out. and let me know if there are any corrections to be made. I rarely build explicit animations, so this project and post really let me dive deep into the classes when I was preparing this post. Thanks for reading.</p>]]></content><author><name></name></author><category term="android," /><category term="flutter" /><summary type="html"><![CDATA[In the previous post, I explained how to get started with explicit animation in Flutter. In this post, I’ll walk you through how to build a simple animation using all the classes I mentioned. We’re going to make a hand-waving animation using the wave hand emoji.]]></summary></entry><entry><title type="html">Explicit Animations in Flutter</title><link href="github.com/emma-ea/android,/flutter/2023/04/16/animating-widgets-flutter.html" rel="alternate" type="text/html" title="Explicit Animations in Flutter" /><published>2023-04-16T16:21:00+00:00</published><updated>2023-04-16T16:21:00+00:00</updated><id>github.com/emma-ea/android,/flutter/2023/04/16/animating-widgets-flutter</id><content type="html" xml:base="github.com/emma-ea/android,/flutter/2023/04/16/animating-widgets-flutter.html"><![CDATA[<p>UI feels pretty cool when a touch of animation is sprinkled all over it. It’s been a couple of days since I started diving into Flutter animations. Most of the animations I do are quite simple and require you to just use a widget, and the animation task is done for you. These widgets are called implicit animations. They are basically animations that are controlled by the framework. These types of animations are mostly used for simple tasks like animating the size change of a container or the opacity or color of a widget.</p>

<p>I want to try something different this time, and that led me into researching explicit animations. With these types of animations, you have mostly full control over the animation, including when it starts, how long it takes, the beginning and ending of the animation values, etc. Explicit animations put animations on steroids, and with much familiarity, you can do amazing things. I have been motivated a lot by demos and showcases being put out there by the community, and I decided to give it a try. Animate a simple thing: a hand wave emoji.</p>

<h2 id="explicit-animation-fundamentals">Explicit animation fundamentals</h2>

<p>To better understand explicit animations, there are a couple of things I first needed to get comfortable with. Explicit animations are basically, as said earlier, about being in full control of the animation; hence, one of the things we need is an animation controller. We also need something to animate and another to hold the beginning and ending values of our animation since our animation is going to play within a finite time.</p>

<h3 id="animation-controller">Animation Controller</h3>

<p>Animation controller is a class responsible for controlling and managing animations. It gives you some functionality such as forwarding (starting), reversing, and stopping the animation. The animation controller also gives you the ability to set the total duration of the animation, so if you want your animation to last 2 minutes, you do it here. To use the animation controller, you first need to use a mixin called <code class="language-plaintext highlighter-rouge">TickerProviderStateMixin</code>. The <code class="language-plaintext highlighter-rouge">TickerProvider</code> class is capable of notifying the controller when a UI frame changes; hence, it is required when dealing with animations since you need to know of UI frame changes.</p>

<h3 id="animation">Animation</h3>

<p>The animation class handles our animating objects. It’s capable of notifying us about the status of the animation, whether it’s running or not, and it lets other objects also listen for changes during the “animation” of the object. The animation consists of a value, and at any point in time, we can know the progress of our animated object. The animation controller extends animation. Hence, you could run animations without necessarily creating an animation object. If you want an animation that you can just run forward and backward, consider only using the animation controller class.</p>

<h3 id="tween">Tween</h3>

<p>Tweens are quite simple; they let you specify the beginning and ending values of our animation. It returns a linear interpolation from the beginning and ending values. Of course, this can be manipulated with curves if you want to go with a non-linear flow in your animation. With Tweens, you can specify which type of value or property of the object you want to manipulate, If you want to animate the colors, you could specify a color tween and animate between different color values.</p>

<p>With these three classes and a bit more comfort in understanding how they work, you can get started animating widgets explicitly.</p>

<h3 id="lets-continue-in-part-2">Let’s continue in part 2</h3>

<p>I felt like this piece would be getting longer, so I have broken it down into two parts. Check out Explicit Animations in Flutter 2 for an example project I worked on.</p>]]></content><author><name></name></author><category term="android," /><category term="flutter" /><summary type="html"><![CDATA[UI feels pretty cool when a touch of animation is sprinkled all over it. It’s been a couple of days since I started diving into Flutter animations. Most of the animations I do are quite simple and require you to just use a widget, and the animation task is done for you. These widgets are called implicit animations. They are basically animations that are controlled by the framework. These types of animations are mostly used for simple tasks like animating the size change of a container or the opacity or color of a widget.]]></summary></entry><entry><title type="html">Learning Vue - Intro to Vue3</title><link href="github.com/emma-ea/web/2023/04/09/learning-vue-intro-stuff.html" rel="alternate" type="text/html" title="Learning Vue - Intro to Vue3" /><published>2023-04-09T17:45:00+00:00</published><updated>2023-04-09T17:45:00+00:00</updated><id>github.com/emma-ea/web/2023/04/09/learning-vue-intro-stuff</id><content type="html" xml:base="github.com/emma-ea/web/2023/04/09/learning-vue-intro-stuff.html"><![CDATA[<p>I have been wanting to learn backend development for a while using Java. I have played around with different frameworks like Django, but I seem to be drawn to using Java, hence why I started learning Spring. But this writeup is not about Java Spring. As a backend engineer. It will be great to have some frontend skills, and being familiar with HTML and CSS, there was no other option but to go with Vue since it’s much more closely tied to HTML. Vue is a Javascript framework for building user interfaces, and if you’re familiar with HTML and CSS, I think it should feel at home for you. With a little knowledge of Javascript, getting started should be a breeze for you.</p>

<h2 id="some-history">Some History</h2>

<p>After a long childish grudge with Javascript, I have finally picked up a Javascript framework for building apps. I remember my first experience with Javascript being quite frustrating; being a beginner and having lots of different opinions as to what to use, what to do, and what not, I got fed up and jumped ship into mobile development. As a beginner in my early programming years, all I wanted was to learn programming, not find myself in cyber arguments with each side saying their framework and conversions are the best. I found the mobile development community to be very linear, and you quickly know what to learn, The learning curve may be steep, but there is a clear layout as to what to do.</p>

<h2 id="lets-goo-vue">Let’s goo Vue</h2>

<p>Back to Vue: Getting started with Vue is much easier than I thought. Just following the documentation on the Vue website quickly helped me setup Vue and run the initial demo project that comes after configuring your Vue project. Vue programming is built around having different units of UI called components. Let’s consider these components as a system that is capable of accepting some inputs called props and sending signals to the environment using events (emitting events). Vue is based on something called component-based programming, and understanding how to fit together components and how they communicate helps you build user interfaces.</p>

<p>I’m still in my first week of learning Vue, and I’m pretty much catching the concepts easily. With Vue built around having components, let me walk you through some sample code to appreciate Vue.</p>

<h2 id="setting-up-your-environment">Setting up your environment</h2>

<p>Vue can be setup by using a package from npm called create-vue. This seems to be the default way of doing it, but you can also run Vue without using these “build-tools”. You can simply import a script to the Vue CDN. Let’s go with the former. In order to set up a Vue project with npm, you need to have npm installed on your system. After installing npm, you can simply setup the project using <code class="language-plaintext highlighter-rouge">npm init vue@latest</code>. This will spawn up the latest Vue package, <code class="language-plaintext highlighter-rouge">create-vue</code> and run the project creation wizard.</p>

<p>The wizard presents you with some options, from which you must choose yes or no. If you’re completely new to Vue or front-end development, go with no for each option. Follow any further instructions to run Vue on a local server that come with project wizard.</p>

<h2 id="vue-components">Vue components</h2>

<p>Vue components are written in “Single-File Component (SFC)” format; this term just describes how you layout your component code. Your component code is separated into <code class="language-plaintext highlighter-rouge">template</code>, <code class="language-plaintext highlighter-rouge">script</code>, and <code class="language-plaintext highlighter-rouge">style</code> sections. To create a Vue component, create a new file and name it with a <code class="language-plaintext highlighter-rouge">.vue</code> extension. Your html code goes into the template section; the script section contains javascript code for rendering html code based on some state or data; and finally, the style section contains styling code.</p>

<p>sample code</p>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">    
// filename: Hello.vue

&lt;script&gt;
    export default {
        data() {
            return {
                text: 'Hello, World'
            }
        }
    }
&lt;/script&gt;

&lt;template&gt;
    // <span class="p">{{</span><span class="w"> </span><span class="nv">code</span><span class="w"> </span><span class="p">}}</span> allows interaction with Javascript expressions
    &lt;p&gt; <span class="p">{{</span><span class="w"> </span><span class="nv">text</span><span class="w"> </span><span class="p">}}</span> &lt;/p&gt;
&lt;/template&gt;

&lt;style&gt;
    p {
        font-weight: bold;
        font-size: 32px;
        color: green;
    }
&lt;/style&gt;</code></pre></figure>

<p>Vue components can be developed either using the <code class="language-plaintext highlighter-rouge">Options</code> API or the <code class="language-plaintext highlighter-rouge">Composition API</code>. These two just define how you describe the component’s logic. With the Options API, you have an object of options such as data, computed, and methods. The Composition API defines your component’s logic using import API functions. The Composition API is seen as being much more concise and letting you do more with less code. But both options are capable, and whoever you choose will be based largely on your preferences. The Options API seems to be more beginner-friendly, and people coming from an OOP background will be able to easily adopt it. The Composition API seems to be tied more to a reactive style of programming.</p>

<p>Sample code for comparison</p>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">    
// Options API
&lt;script&gt;
    export default {
        data() {
            return {
                text: 'Hello'
            }
        }
    }   
&lt;/script&gt;

&lt;template&gt;
    &lt;h1&gt;<span class="p">{{</span><span class="w"> </span><span class="nv">text</span><span class="w"> </span><span class="p">}}</span>&lt;/h1&gt;
&lt;/template&gt;

    </code></pre></figure>

<figure class="highlight"><pre><code class="language-liquid" data-lang="liquid">    
// Composition API
&lt;script&gt;
    import { ref } from 'vue'
    // store text value in a reactive state
    const text = ref('Hello')
&lt;/script&gt;

&lt;template&gt;
    &lt;h1&gt;<span class="p">{{</span><span class="w"> </span><span class="nv">text</span><span class="w"> </span><span class="p">}}</span>&lt;/h1&gt;
&lt;/template&gt;

    </code></pre></figure>

<p>These sample codes above describe what’s known as declarative rendering, which is a core Vue feature where, using template syntax extending HTML, we describe how the HTML should look based on Javascript state, and when the state changes, HTML automatically updates itself.</p>

<h2 id="lets-end-here">Let’s end here</h2>

<p>Vue declarative and component-based styles of building user interfaces should be easy to pick up for anybody wanting a framework with a low learning curve and low barrier of entry. As I keep on learning Vue, I will dive into more Vue features and document everything I have been learning here. So far, I have been liking the experience, and I really intend on gaining some vue skills.</p>]]></content><author><name></name></author><category term="web" /><summary type="html"><![CDATA[I have been wanting to learn backend development for a while using Java. I have played around with different frameworks like Django, but I seem to be drawn to using Java, hence why I started learning Spring. But this writeup is not about Java Spring. As a backend engineer. It will be great to have some frontend skills, and being familiar with HTML and CSS, there was no other option but to go with Vue since it’s much more closely tied to HTML. Vue is a Javascript framework for building user interfaces, and if you’re familiar with HTML and CSS, I think it should feel at home for you. With a little knowledge of Javascript, getting started should be a breeze for you.]]></summary></entry><entry><title type="html">Working with Websockets</title><link href="github.com/emma-ea/android,/flutter/2022/12/12/working-with-websockets.html" rel="alternate" type="text/html" title="Working with Websockets" /><published>2022-12-12T19:27:00+00:00</published><updated>2022-12-12T19:27:00+00:00</updated><id>github.com/emma-ea/android,/flutter/2022/12/12/working-with-websockets</id><content type="html" xml:base="github.com/emma-ea/android,/flutter/2022/12/12/working-with-websockets.html"><![CDATA[<p>I recently got to work on a Flutter project, and I had to use websockets. Being the first time using the protocol, I initially
a couple of mistakes, like using the HTTP protocol scheme as part of the URL scheme. I know you’re laughing at me now,
but it was my first time implementing websockets in an app.</p>

<p>With the idea that most things work similarly, I went ahead without looking much into documentation or articles as to how
websockets work. After some Googling, I learned enough about websockets to get my application running, and I think websockets is
a cool protocol that you should use if you have a suitable use case.</p>

<h3 id="define-websockets">Define Websockets</h3>

<p>Websockets is a communication protocol that is considered an upgrade over HTTP. The main catch with web sockets is that, unlike
traditional HTTP connections, where after a client receives a response from a request it makes to a server, the server closes
the connection. There are other techniques for persisting or keeping the connection alive, but as I studied more
into Websockets and HTTP protocols, I got to know this “keep-alive” setting for HTTP protocols may have its downsides.</p>

<p>Going back to web sockets, I hope to cover more on that.The entire idea behind websockets is that once a connection is created 
with the server, There is a bi-directional, full-duplex communication link established. This comes with the perk of being 
low-latent. <code class="language-plaintext highlighter-rouge">Low latency in computer networks mean there is minimal delay in processing volumes of data messages.</code> Hence its suitable 
for online multiplayer games, chat applications, sports or financial applications where change in data should be readily available 
to users/clients.</p>

<h3 id="style">Style</h3>

<p>Unlike HTTP, websockets also have their own url scheme, which helps identify websocket connections.
An example in Dart/Flutter:</p>

<figure class="highlight"><pre><code class="language-dart" data-lang="dart"><span class="c1">// parse uri</span>
<span class="n">val</span> <span class="n">uri</span> <span class="o">=</span> <span class="n">Uri</span><span class="o">.</span><span class="na">parse</span><span class="o">(</span><span class="s">"ws://some-realtime-app"</span><span class="o">);</span>
<span class="n">val</span> <span class="n">channel</span> <span class="o">=</span> <span class="n">IOWebSocketChannel</span><span class="o">.</span><span class="na">connect</span><span class="o">(</span><span class="n">uri</span><span class="o">);</span>

<span class="c1">// send message</span>
<span class="n">channel</span><span class="o">.</span><span class="na">sink</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"some-message-to-server"</span><span class="o">);</span>

<span class="c1">// listen to server</span>
<span class="n">channel</span><span class="o">.</span><span class="na">stream</span><span class="o">.</span><span class="na">listen</span><span class="o">((</span><span class="n">event</span><span class="o">){</span> <span class="cm">/* handle some response stream */</span> <span class="o">});</span></code></pre></figure>

<p>This connection establishment happens over a single TCP connection, and attaching event handlers allows you to know the state of
of the connection. In Dart/Flutter, the web_socket_channel package provides a cross-platform wrapper to use websocket connections.
on Android and iOS devices. Take a look at the package on Flutter pub.dev if you want to play around with it.</p>

<h3 id="websockets-usecases">WebSockets Usecases</h3>

<p>Because most applications require real-time data to be consumed by their clients, it is critical that you understand the use case of 
the application or project and select the appropriate technology. I’ve attempted to list a couple of use cases that require 
websockets implementation below: </p>

<ul>
  <li>Financial tickers</li>
  <li>Multiplayer games</li>
  <li>Sports updates</li>
  <li>Social Feeds / Messaging / Chat applications</li>
  <li>Location-based apps</li>
</ul>

<h3 id="my-take">My take</h3>

<p>Initially not being familiar with websockets, I intended on building the project that introduced me to websockets by using a
technique called Periodic Polling,  where after a certain duration, the client device makes a request to the server for new data,
If any, the fetch data is updated. Actually, this was my initial idea for solving the issue of getting real-time data.</p>

<p>This not only has some downsides like HTTP overhead, constant querying for new data. But it would also take additional time.
implementing this periodic scheduling for data. I’m fairly new to this, and after reading in a few places, I discovered
there are other methods, like <code class="language-plaintext highlighter-rouge">Long Polling</code> by which the connection to the server is kept alive and the server returns
a “new” response after a certain period.</p>

<p>Looking at websockets, once the connection is established and messages are sent to the server. There will be no need to constantly 
query the server for new data, unless a different message is sent for new data. You just have to setup a listener, and once new 
data becomes available, it is sent to its consumers.</p>

<p>To end here, I think Websockets are a cool protocol. I hope to use it more and experiment with it. see you in the next one.</p>]]></content><author><name></name></author><category term="android," /><category term="flutter" /><summary type="html"><![CDATA[I recently got to work on a Flutter project, and I had to use websockets. Being the first time using the protocol, I initially a couple of mistakes, like using the HTTP protocol scheme as part of the URL scheme. I know you’re laughing at me now, but it was my first time implementing websockets in an app.]]></summary></entry><entry><title type="html">A/B System Updates Are Really Cool</title><link href="github.com/emma-ea/android/2022/11/26/ab-system-updates-are-cool.html" rel="alternate" type="text/html" title="A/B System Updates Are Really Cool" /><published>2022-11-26T00:32:00+00:00</published><updated>2022-11-26T00:32:00+00:00</updated><id>github.com/emma-ea/android/2022/11/26/ab-system-updates-are-cool</id><content type="html" xml:base="github.com/emma-ea/android/2022/11/26/ab-system-updates-are-cool.html"><![CDATA[<p>One of the things that got me into programming, like most people, was movies. In my books, I’ve been a hacker-type programmer.
tweaking every technology I can lay my hands on. well, including my mobile device. Running custom roms like Lineage or flashing Kali Net Hunter
and going extreme with Ubuntu Touch. a variant of Ubuntu optimized and built to run on Android devices.</p>

<p>In light of the foregoing, I have had the opportunity to successfully root my device in order to run some of these custom roms on my Android.
device. Well, I soft-bricked the phone on a number of occasions.</p>

<p>The weekend is here, and to do something hacky this time, I decided to root my current phone. I was even shocked that I haven’t done so. Damn, I have lost my powers.
To root it, I first needed to install TWRP’s custom recovery image, which will allow me to boot into a custom recovery menu on my Android device.</p>

<p>It gives me more tools to do things right away that the stock recovery that comes with devices does not.An example is that it makes installing custom roms much easier.</p>

<p>But I’m not here to talk about Android roms and hacking Android. I’m still a beginner tinkerer here.</p>

<p>with every device having its own set of instructions on how to root it. During my research, I stumbled on A/B system updates. and I think it’s really cool.
I wanted to write a short piece about it.</p>

<p>How did I get to know this? I needed to know the partition scheme of the target device in order to install twrp.This basically means that every
An Android device has a structure or rule according to which its internal ROM is split up for data.
These partitions could hold files like boot files; a partition could be setup for where logical drives are stored; etc.</p>

<p>Two partition schemes came up in system updates: A/B and Legacy A. Let’s talk about A and  B, and you can make references as to how Legacy A could work.</p>

<p>A/B system updates are also called seamless updates. This is due to how system updates, or OTA updates, are downloaded and patched into the operating system.
With A/B, the system uses two partitions. The android system runs on one slot, which is called the current slot, and the other, unused slot, resides on the app.
with the A (running system) slot having no access to it.</p>

<p>During OTA updates, the current slot is updated, and the goal of the system B slot is to act as a failsafe for the operating system to fallback to in case the update breaks.
the current system This could be the result of an error during or after an update.</p>

<p>The android system, or if I could say it, the bootloader, decides which slot or partition to run based on a bootable attribute that determines the state of the system slot.
marking it as safe for booting from it.</p>

<p>Once the update is done, the A/B system has background jobs that prepare the system to boot into the updated version. to learn more about A/B system updates
Check out the documentation on the source.android.com site.</p>

<p>But here I want to talk a little about A/B system updates and how I was fascinated by it when I learned something like this was happening on a user’s device.
It’s not happening on mine, at least.How did I know? Well, there is an adb command that lets you check if your device uses the legacy A or A/B system updates.</p>

<p>The idea behind the new A/B system updates is to give ideas on how to write software for mobile devices or apps that are capable of withstanding severe error issues.
It’s quite simple to consider.have two copies of the system. make one of the partitions usable due to system updates or bugs that cause problems
the current system, fallback to a more stable version.</p>

<p>I’m here thinking about ways I could write software, borrowing ideas from this system update concept.
As we build systems that are consumer-facing, it’s necessary that our systems stay online as much as possible with minimal issues. Certain cataclysms could
cause data loss or make a system dysfunctional, but the systems we build should be able to bounce back after all these.</p>

<p>I hope you learned something from this. I’m just ranting about how cool I think this is and how it has added a new perspective on how to approach and build systems.</p>

<p>I’m going to have to leave you here; keep hacking.</p>]]></content><author><name></name></author><category term="android" /><summary type="html"><![CDATA[One of the things that got me into programming, like most people, was movies. In my books, I’ve been a hacker-type programmer. tweaking every technology I can lay my hands on. well, including my mobile device. Running custom roms like Lineage or flashing Kali Net Hunter and going extreme with Ubuntu Touch. a variant of Ubuntu optimized and built to run on Android devices.]]></summary></entry></feed>